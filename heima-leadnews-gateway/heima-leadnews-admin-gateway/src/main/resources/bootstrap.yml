server:
  port: 53613
spring:
  application:
    name: leadnews-admin-gateway
  cloud:
    nacos:
      discovery:
        server-addr: localhost:6848
      config:
        server-addr: localhost:6848
        file-extension: yml