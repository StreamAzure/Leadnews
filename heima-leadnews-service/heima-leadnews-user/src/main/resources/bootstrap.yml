server:
  port: 52811
spring:
  application:
    name: leadnews-user
  cloud:
    nacos:
      discovery:
        server-addr: localhost:6848
      config:
        server-addr: localhost:6848
        file-extension: yml