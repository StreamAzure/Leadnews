server:
  port: 53611
spring:
  application:
    name: leadnews-app-gateway
  cloud:
    nacos:
      discovery:
        server-addr: localhost:6848
      config:
        server-addr: localhost:6848
        file-extension: yml