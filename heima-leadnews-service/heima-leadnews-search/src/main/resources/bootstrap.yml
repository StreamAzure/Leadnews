server:
  port: 52816
spring:
  application:
    name: leadnews-search
  cloud:
    nacos:
      discovery:
        server-addr: localhost:6848
      config:
        server-addr: localhost:6848
        file-extension: yml